spring:
  config:
    activate:
      on-profile: test
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:mysql:5.7.35:///localhost:33006/ticker?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    # &TC_INITSCRIPT=file:src/test/resources/schema.sql
    username: root
    password: test
